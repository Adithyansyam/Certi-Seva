{
  "buildCommand": "cd vite-project && npm install && npm run build && mkdir -p ../dist && cp -r dist/* ../dist/",
  "outputDirectory": "dist",
  "installCommand": "echo 'skip'",
  "github": {
    "silent": true
  }
}